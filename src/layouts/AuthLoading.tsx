import React, {ReactElement} from 'react';
import '../styles/layouts/auth-loading.scss';

const AuthLoading: React.FC = (): ReactElement => {
  return (
    <div className="auth-loading" />
  )
}

export default AuthLoading;
